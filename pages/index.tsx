import { Layout } from '../components/Layout';
import { EstatesList } from '../components/Estates/EstatesList';

const Home = () => {
  return (
    <Layout>
      <EstatesList />
    </Layout>
  );
};

export default Home;
